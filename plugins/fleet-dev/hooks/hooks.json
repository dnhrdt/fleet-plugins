{
  "description": "Fleet-dev hooks for Windows/Git Bash compatibility and skill enforcement",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/block-npm-npx.js\"",
            "timeout": 5
          },
          {
            "type": "prompt",
            "prompt": "Does this command contain both 'gh' and 'issue'? If yes, set permissionDecision to 'ask' and add systemMessage reminding to use Fleet skills: review-issues for list/view, research for create in claude-research, feedback for create elsewhere. If no, set permissionDecision to 'allow'.",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
