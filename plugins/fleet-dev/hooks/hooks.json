{
  "description": "Fleet-dev hooks for Windows/Git Bash compatibility and skill enforcement",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/block-npm-npx.js\"",
            "timeout": 5
          },
          {
            "type": "prompt",
            "prompt": "Command: $TOOL_INPUT.command. If command contains 'gh' and 'issue', return 'ask' with reason: Use Fleet skills instead (review-issues for list/view, research for create in claude-research, feedback for create elsewhere). Otherwise return 'approve'.",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
